#include "holberton.h"

/**
 * mul - check the isdigit case.
 * @a: variable number a 
 * @b: variable number b
 * Return: Always 0.
 */
int mul(int a, int b)

{
	int result;

	result = (a * b);
	return (result);
}
