#include "holberton.h"
#include <stdio.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int print_alphabet(void)
char c;for (c = 'A' ; c <= 'Z' ; c++)
short i = 0;
while (string[c]!= '\0')
{
short c = 0;
while (string[c]!= '\0')
{
_putchar(string[c]);
i++;
{
_putchar (c);
}
return (0);
}
